---
phase: 03-brisbane-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/supabase/migrations/002_postgis_schema.sql
autonomous: true
requirements:
  - EARTH-07

must_haves:
  truths:
    - "PostGIS extension is enabled in Supabase"
    - "flood_zones table exists with geometry(MultiPolygon, 4326) column and GIST index"
    - "flood_forecasts table exists with generated location_hash column and UNIQUE constraint"
    - "displacement_records table exists with country_code + year composite index"
    - "analysis_cache table exists with UNIQUE(region_slug, analysis_type, params_hash)"
    - "flood_zones_in_bbox PostGIS function exists for bbox spatial queries"
  artifacts:
    - path: "web/supabase/migrations/002_postgis_schema.sql"
      provides: "PostGIS schema — 4 tables, indexes, RLS policies, flood_zones_in_bbox function"
      contains: "CREATE EXTENSION IF NOT EXISTS postgis"
  key_links:
    - from: "web/supabase/migrations/002_postgis_schema.sql"
      to: "Supabase remote database"
      via: "supabase db push or manual SQL execution"
      pattern: "flood_zones.*region_slug.*brisbane|grantham|niue|tuvalu"
    - from: "web/app/api/flood-zones/route.ts"
      to: "flood_zones_in_bbox"
      via: "supabase.rpc('flood_zones_in_bbox', ...)"
      pattern: "flood_zones_in_bbox"
---

<objective>
Deploy the Supabase PostGIS schema that all Phase 3 ingestion scripts and API routes depend on.

Purpose: Without this schema, no ingestion script can upsert data and no API route can query spatial data. This is the dependency root for all other Phase 3 plans.
Output: web/supabase/migrations/002_postgis_schema.sql — a single migration file that creates PostGIS extension + 4 tables + indexes + RLS policies + flood_zones_in_bbox function, ready to run against the Supabase project (vofpmfxqlflabpdackls.supabase.co).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-brisbane-data-pipeline/03-CONTEXT.md
@.planning/ROADMAP.md
@web/supabase/migrations/001_profiles.sql
@web/lib/regions.ts

<interfaces>
<!-- Region slugs that MUST appear in region_slug column values (from lib/regions.ts) -->
<!-- Valid slugs: niue, tuvalu, fiji, vanuatu, solomon-islands, palau, kiribati,
     marshall-islands, brisbane, grantham -->
<!-- Valid country_codes: NU, TV, FJ, VU, SB, PW, KI, MH, AU -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PostGIS migration SQL</name>
  <files>web/supabase/migrations/002_postgis_schema.sql</files>
  <read_first>
    - web/supabase/migrations/001_profiles.sql (migration style — naming, RLS pattern, trigger pattern)
    - .planning/phases/03-brisbane-data-pipeline/03-CONTEXT.md (LOCKED schema under "Supabase PostGIS Schema")
    - web/lib/regions.ts (valid slugs + country codes for comments)
  </read_first>
  <action>
Create `web/supabase/migrations/002_postgis_schema.sql` with EXACTLY the following content (schema is locked in CONTEXT.md — do not alter table or column names):

```sql
-- Enable PostGIS
CREATE EXTENSION IF NOT EXISTS postgis;

-- ============================================================
-- flood_zones
-- Sources: 'bcc', 'qld_2011', 'qfao'
-- region_slug matches lib/regions.ts slugs exactly
-- ============================================================
CREATE TABLE IF NOT EXISTS flood_zones (
  id                        uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  source                    text NOT NULL,
  flood_class               text,
  annual_recurrence_interval integer,
  council                   text,
  country_code              char(2),
  region_slug               text,
  geometry                  geometry(MultiPolygon, 4326),
  data_date                 date,
  ingested_at               timestamptz DEFAULT now(),
  UNIQUE(source, flood_class, council)
);
CREATE INDEX IF NOT EXISTS flood_zones_geom_idx ON flood_zones USING GIST (geometry);
CREATE INDEX IF NOT EXISTS flood_zones_region_idx ON flood_zones (region_slug);

-- RLS: public read, no public write
ALTER TABLE flood_zones ENABLE ROW LEVEL SECURITY;
CREATE POLICY "flood_zones_public_read" ON flood_zones FOR SELECT USING (true);
CREATE POLICY "flood_zones_service_write" ON flood_zones FOR ALL
  USING (auth.role() = 'service_role');

-- ============================================================
-- flood_zones_in_bbox
-- Returns flood zones intersecting a lon/lat bounding box.
-- Used by: web/app/api/flood-zones/route.ts via supabase.rpc()
-- ============================================================
CREATE OR REPLACE FUNCTION flood_zones_in_bbox(
  p_minx numeric,
  p_miny numeric,
  p_maxx numeric,
  p_maxy numeric
)
RETURNS SETOF flood_zones
LANGUAGE sql
STABLE
AS $$
  SELECT *
  FROM flood_zones
  WHERE ST_Intersects(
    geometry,
    ST_MakeEnvelope(p_minx, p_miny, p_maxx, p_maxy, 4326)
  );
$$;

-- ============================================================
-- flood_forecasts
-- Sources: 'open_meteo', 'glofas_ewds', 'wmip', 'deltares', 'jrc_glofas'
-- ============================================================
CREATE TABLE IF NOT EXISTS flood_forecasts (
  id                    uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  source                text NOT NULL DEFAULT 'open_meteo',
  forecast_date         date NOT NULL,
  location              geometry(Point, 4326),
  latitude              numeric,
  longitude             numeric,
  discharge_m3s         numeric,
  return_period_years   integer,
  inundation_depth_m    numeric,
  coastal_depth_m       numeric,
  scenario              text,
  location_hash         text GENERATED ALWAYS AS (
    encode(sha256((source || forecast_date::text || latitude::text || longitude::text)::bytea), 'hex')
  ) STORED,
  ingested_at           timestamptz DEFAULT now(),
  UNIQUE(source, forecast_date, location_hash)
);
CREATE INDEX IF NOT EXISTS flood_forecasts_geom_idx ON flood_forecasts USING GIST (location);
CREATE INDEX IF NOT EXISTS flood_forecasts_date_idx ON flood_forecasts (forecast_date DESC);

ALTER TABLE flood_forecasts ENABLE ROW LEVEL SECURITY;
CREATE POLICY "flood_forecasts_public_read" ON flood_forecasts FOR SELECT USING (true);
CREATE POLICY "flood_forecasts_service_write" ON flood_forecasts FOR ALL
  USING (auth.role() = 'service_role');

-- ============================================================
-- displacement_records
-- Sources: 'idmc', 'worldbank', 'pdh_stat'
-- country_code: ISO 3166-1 alpha-2 (NU, TV, FJ, VU, SB, PW, KI, MH, PG, TO, WS, AU)
-- ============================================================
CREATE TABLE IF NOT EXISTS displacement_records (
  id               uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  source           text NOT NULL,
  country_code     char(2) NOT NULL,
  country_name     text,
  event_date       date,
  year             integer,
  cause            text,
  displaced_count  integer,
  net_migration    integer,
  population       integer,
  location         geometry(Point, 4326),
  admin1           text,
  data_type        text,
  ingested_at      timestamptz DEFAULT now()
);
CREATE INDEX IF NOT EXISTS displacement_country_idx ON displacement_records (country_code, year DESC);
CREATE INDEX IF NOT EXISTS displacement_geom_idx ON displacement_records USING GIST (location);

ALTER TABLE displacement_records ENABLE ROW LEVEL SECURITY;
CREATE POLICY "displacement_public_read" ON displacement_records FOR SELECT USING (true);
CREATE POLICY "displacement_service_write" ON displacement_records FOR ALL
  USING (auth.role() = 'service_role');

-- ============================================================
-- analysis_cache
-- analysis_type: 'coastline_change', 'flood_extent', 'flood_depth'
-- status: 'pending', 'running', 'complete', 'failed'
-- ============================================================
CREATE TABLE IF NOT EXISTS analysis_cache (
  id            uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  country_code  char(2),
  region_slug   text,
  analysis_type text NOT NULL,
  params_hash   text NOT NULL,
  status        text DEFAULT 'pending',
  result        jsonb,
  error         text,
  created_at    timestamptz DEFAULT now(),
  completed_at  timestamptz,
  UNIQUE(region_slug, analysis_type, params_hash)
);

ALTER TABLE analysis_cache ENABLE ROW LEVEL SECURITY;
CREATE POLICY "analysis_cache_public_read" ON analysis_cache FOR SELECT USING (true);
CREATE POLICY "analysis_cache_service_write" ON analysis_cache FOR ALL
  USING (auth.role() = 'service_role');
```

After writing the file, apply it to the remote Supabase project:
```bash
cd web && npx supabase db push
```

If `supabase db push` fails (CLI not installed or linked), output the SQL file path and instruct the user to run it manually in the Supabase SQL editor at https://supabase.com/dashboard/project/vofpmfxqlflabpdackls/sql/new. Do NOT silently skip — surface the failure clearly.
  </action>
  <verify>
    <automated>
      # Check file exists with all 4 CREATE TABLE statements
      grep -c "CREATE TABLE" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
      # Expected output: 4

      # Check PostGIS extension line present
      grep "CREATE EXTENSION IF NOT EXISTS postgis" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql

      # Check flood_zones_in_bbox function present
      grep "CREATE OR REPLACE FUNCTION flood_zones_in_bbox" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql

      # Check all 4 GIST indexes present
      grep "USING GIST" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql

      # Check generated column present in flood_forecasts
      grep "location_hash" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql

      # Check UNIQUE constraints present
      grep -c "UNIQUE" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
      # Expected: 4 (one per table)
    </automated>
  </verify>
  <acceptance_criteria>
    - File `web/supabase/migrations/002_postgis_schema.sql` exists
    - `grep -c "CREATE TABLE" ...` returns exactly 4
    - `grep "CREATE EXTENSION IF NOT EXISTS postgis"` returns a match
    - `grep "CREATE OR REPLACE FUNCTION flood_zones_in_bbox"` returns a match
    - `grep "USING GIST" ...` returns 3 matches (flood_zones, flood_forecasts, displacement_records)
    - `grep "location_hash" ...` returns a match (generated column in flood_forecasts)
    - `grep -c "UNIQUE" ...` returns exactly 4
    - `grep "region_slug" ...` returns matches in flood_zones AND analysis_cache (both tables that use it)
    - RLS enabled on all 4 tables (`grep -c "ENABLE ROW LEVEL SECURITY"` = 4)
  </acceptance_criteria>
  <done>Migration file exists, passes all grep checks including flood_zones_in_bbox function, and has been applied to Supabase (or user has been given the SQL to apply manually)</done>
</task>

</tasks>

<verification>
Run the following after task completes:
```bash
grep -c "CREATE TABLE" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
# Must return: 4
grep -c "ENABLE ROW LEVEL SECURITY" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
# Must return: 4
grep "CREATE EXTENSION IF NOT EXISTS postgis" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
# Must return a match
grep "CREATE OR REPLACE FUNCTION flood_zones_in_bbox" C:/Dev/deqode-earth/web/supabase/migrations/002_postgis_schema.sql
# Must return a match
```
</verification>

<success_criteria>
- web/supabase/migrations/002_postgis_schema.sql exists with PostGIS extension + 4 tables + GIST indexes + UNIQUE constraints + RLS policies + flood_zones_in_bbox function
- Schema exactly matches the locked spec in 03-CONTEXT.md
- Migration has been applied to the Supabase project (or file is ready for manual application)
</success_criteria>

<output>
After completion, create `.planning/phases/03-brisbane-data-pipeline/03-01-SUMMARY.md` with:
- Tables created and their key columns
- flood_zones_in_bbox function signature
- Migration file path
- Whether db push succeeded or manual SQL application is needed
- Any deviations from spec (there should be none)
</output>
